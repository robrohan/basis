(load "test_data/mod_transformer.lisp")
(load-gguf "models/gpt2.Q4_0.gguf")
(load-gguf-vocab "models/gpt2.Q4_0.gguf")

(define n-embd   768)
(define n-heads  12)
(define head-dim 64)
(define n-ff     3072)
(define n-vocab  50257)
(define eps      0.00001)

(define gpt2-ln (lambda (x w b) (+ (* (layer-norm x eps) w) b)))
(define embed (lambda (tok pos)
    (+ (slice token_embd.weight tok)
       (slice position_embd.weight pos))))
(define col-range (lambda (M start end)
    (T (slice-range (T M) start end))))
(define sdpa (lambda (Qh Kh Vh mask)
    (let* (sc (+ (/ (@ Qh (T Kh)) (sqrt head-dim)) mask))
    (@ (softmax sc) Vh))))
(define head-t (lambda (Q K V mask h)
    (let* (s (* h head-dim))
    (let* (e (+ s head-dim))
    (T (sdpa (col-range Q s e)
             (col-range K s e)
             (col-range V s e)
             mask))))))
(define head-stack (lambda (Q K V mask h)
    (let* (hout (head-t Q K V mask h))
    (cond
        ((< h 11) (vstack hout (head-stack Q K V mask (+ h 1))))
        (#t hout)))))
(define gpt2-attn (lambda (x Wqkv bqkv Wo bo)
    (let* (qkv  (+ (@ x (T Wqkv)) bqkv))
    (let* (Q    (col-range qkv 0       n-embd))
    (let* (K    (col-range qkv n-embd  (* 2 n-embd)))
    (let* (V    (col-range qkv (* 2 n-embd) (* 3 n-embd)))
    (let* (seq  (slice (shape x) 0))
    (let* (mask (causal-mask seq))
    (let* (out  (T (head-stack Q K V mask 0)))
    (+ (@ out (T Wo)) bo))))))))))
(define gpt2-ff (lambda (x Wup bup Wdown bdown)
    (+ (@ (gelu (+ (@ x (T Wup)) bup)) (T Wdown)) bdown)))
(define gpt2-block (lambda (x Wln1 bln1 Wqkv bqkv Wo bo Wln2 bln2 Wup bup Wdown bdown)
    (let* (a  (gpt2-attn (gpt2-ln x Wln1 bln1) Wqkv bqkv Wo bo))
    (let* (x1 (+ x a))
    (let* (f  (gpt2-ff (gpt2-ln x1 Wln2 bln2) Wup bup Wdown bdown))
    (+ x1 f))))))
(define gpt2-forward (lambda (x)
    (let* (x      (run-blocks x))
    (let* (seq    (slice (shape x) 0))
    (let* (last-h (reshape (slice x (- seq 1)) [1 768]))
    (let* (normed (gpt2-ln last-h output_norm.weight output_norm.bias))
    (let* (logits (@ normed (T output.weight)))
    (argmax logits))))))))

(load "test_data/gpt2_generate.lisp")

(define build-input-loop (lambda (toks pos n acc)
    (cond
        ((< pos n) (build-input-loop toks (+ pos 1) n
                    (vstack acc (reshape (embed (slice toks pos) pos) [1 768]))))
        (#t acc))))
(define build-input (lambda (tok-tensor)
    (let* (n (slice (shape tok-tensor) 0))
    (build-input-loop tok-tensor 1 n
        (reshape (embed (slice tok-tensor 0) 0) [1 768])))))

;; Test WITHOUT trailing space
(define toks-nospace (tokenize "once upon a time"))
(print "tokens (no trailing space):") (print toks-nospace)
(define ctx-nospace (build-input toks-nospace))
(define pred-nospace (gpt2-forward ctx-nospace))
(print "predicted id (no space):") (print pred-nospace)
(print "predicted str (no space):") (print (token->str pred-nospace))

;; Test WITH trailing space (original)
(define toks-space (tokenize "once upon a time "))
(print "tokens (with space):") (print toks-space)
(define ctx-space (build-input toks-space))
(define pred-space (gpt2-forward ctx-space))
(print "predicted id (with space):") (print pred-space)
(print "predicted str (with space):") (print (token->str pred-space))
